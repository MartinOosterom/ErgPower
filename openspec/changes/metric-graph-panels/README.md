# metric-graph-panels

Make every measured value showable as a **value tile** or a **value-vs-time graph**, both driven by one
metric registry that declares, per measurement, which display modes are meaningful — replacing the
hardcoded 3-metric graph limit.
